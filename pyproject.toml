[project]
name = "oryzawatch"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "djangorestframework-stubs>=3.18.0",
    "numpy",
    "Pillow",
    "tensorflow-cpu",
    "torch",
    "torchvision",
]

# CPU-only torch/torchvision wheels come from the PyTorch index:
#   uv pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
