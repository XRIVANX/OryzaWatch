Django==6.0.6
djangorestframework
djangorestframework-simplejwt
django-cors-headers
python-decouple
Pillow
numpy
mysqlclient
whitenoise
gunicorn
# AI leaf scanner. PyTorch is the primary inference backend; tensorflow-cpu is the
# fallback backend and the source for the mobile TFLite export (manage.py export_tflite).
# Install the CPU torch build from the PyTorch index, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
tensorflow-cpu
torch
torchvision
# Lesion segmentation (train_leaf_segmentation): U-Net w/ pretrained encoders.
segmentation-models-pytorch
# Connected-component -> bounding-box derivation for the YOLO dataset builder.
scipy
# Lesion detection (train_leaf_yolo / diagnostics.ai lesion boxes).
ultralytics
